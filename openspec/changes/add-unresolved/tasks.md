## 1. Core

- [x] 1.1 `internal/resolve/unresolved.go`: `Unresolved(e, opts)` with the selection rule, the status classification from a dry resolution, and the ordering

## 2. Front-ends

- [x] 2.1 `intentions unresolved` verb with `--subject`, resolver and act flags; text and JSON output
- [x] 2.2 `unresolved` MCP tool; instructions header and bundle manifest updated to 20 tools
- [x] 2.3 Tests: CLI scenarios (ready, blocked, incomplete, exclusions, subject filter) and the MCP round trip

## 3. Docs and release

- [x] 3.1 Skill loop and verb table; README verb row; `docs/mcp.md` tool table and counts; CHANGELOG 0.6.0
- [x] 3.2 Tag `v0.6.0` and verify the release
