## 1. Store

- [x] 1.1 `WritePointer` becomes idempotent on identical content and refuses a different target with exit code 1

## 2. CLI

- [x] 2.1 `workspace pointer [workspace-dir] [--at] [--force]` with the relative/absolute rule and the three usage guards
- [x] 2.2 `init --pointer`
- [x] 2.3 Tests for both, including the refusal, `--force`, and resolution through a written pointer

## 3. Docs

- [x] 3.1 README verb row and discovery note; the skill's setup section; CHANGELOG 0.7.0
